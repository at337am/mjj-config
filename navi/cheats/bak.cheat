% bak, hash

# 校验哈希清单
sha256sum -c SHA256SUMS

% bak, hash, fd

# 批量; 在当前路径下, 递归所有文件, 生成哈希清单 SHA256SUMS
fd -t f --exclude 'SHA256SUMS' -x sha256sum {} > SHA256SUMS

% bak, split

# 将一个文件拆分为多个分片 (指定数量和输出名)
split -d -n 4 input.tar input.tar_part_

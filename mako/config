# 注意: mako 配置不支持行内注释!

# 默认字体
font=Adwaita Sans SemiBold 12

# 默认悬浮在屏幕最上方
layer=overlay

# 默认通知出现的位置
# 可选: top-right, top-center, top-left, bottom-right, bottom-center, bottom-left, center
anchor=top-right

width=250
height=375
margin=12
padding=12
border-size=0
border-radius=0

# 默认颜色
background-color=#2e2e2ed9
border-color=#e5e5e5
text-color=#e5e5e5

# 默认格式化: %s 标题, %b 正文, \n 换行
format=%s\n%b

# 默认消失时间 (单位: 毫秒)
default-timeout=3000

# 音量, 麦克风, 提示词
[app-name=prompts]
anchor=bottom-center
margin=0

[app-name=volume]
anchor=bottom-center
margin=0

[app-name=microphone]
anchor=bottom-center
margin=0

[urgency=low]
# 低优先级通知 (例如: 音乐换曲)
default-timeout=1500

[urgency=critical]
# 重要通知 (例如: 电量低警告)
background-color=#ff8282d9
# 0 代表永不自动消失，需要手动关闭
default-timeout=0

# 测试:
# makoctl reload && notify-send "测试标题" "这是一个简洁的直角风格通知"
# notify-send -u low "低级通知" "这是一个低级通知"
# notify-send -u critical "紧急！" "这是一个紧急通知"

# 只有当 Firefox 发送的通知标题中包含 "Download" 时才触发
# [app-name=Firefox summary="*Download*"]
